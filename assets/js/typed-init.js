(function($) {
	$(document).ready(function(e) {
		$('.page-subheader').typed({
			strings: ['Allow me to introduce myself!'],
			typeSpeed: 40,
			cursorChar: '|'
		});
	}); // document.ready
})(jQuery);
